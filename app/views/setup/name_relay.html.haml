%section.row
  .small-12.columns
    %h2=t 'setup.heading'
    %p=t '.choose_a_name'
    =form_for @relay, url: wizard_path, method: :put do |f|
      =f.text_field :name
      =f.submit t('setup.save_button')
