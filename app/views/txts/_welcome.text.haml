welcome to #{defined?(target_relay) && target_relay.name.present? ? "relay #{target_relay.name}" : "the relay"}. yr name: #{name}. #{render partial: 'txts/commands_content', formats: [:text], locals: {subscriber_count: subscriber_count}}
